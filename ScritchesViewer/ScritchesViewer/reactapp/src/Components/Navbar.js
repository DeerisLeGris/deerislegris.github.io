import { Flex } from "@chakra-ui/react";

function Navbar({onImageSelected}) {
    return(
        <Flex flexGrow={0} flexShrink={0} flexBasis="500px" backgroundColor="red">

        </Flex>
    )
}

export default Navbar;